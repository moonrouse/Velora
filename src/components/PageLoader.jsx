function PageLoader() {
  return (
    <section className="status-block center-screen">
      <div className="spinner" aria-hidden="true" />
      <p>Loading...</p>
    </section>
  );
}

export default PageLoader;
